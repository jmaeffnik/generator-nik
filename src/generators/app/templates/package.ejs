{
  "name": "<%=appName %>",
  "version": "<%=version %>",
  "description": "<%=description %>",
  "scripts": {
    "build": "gulp build",
    "test:ci-unit": "gulp testCIUnit",
    "test:ci-e2e": "gulp testCIE2e",
    "bump": "npm version patch",
    "clean": "gulp clean"
  }
}
